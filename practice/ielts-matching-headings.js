(() => {
  'use strict';
  const DATA_PATHS = { academic: '../data/ielts-academic/reading/matching-headings.json', general: '../data/ielts-general/reading/matching-headings.json' };
  const EXAM_CONFIG = {
    academic: { name: 'IELTS Academic', title: 'Academic Matching Headings', hub: '../pages/ielts-academic.html', hubLabel: 'Back to IELTS Academic Hub' },
    general: { name: 'IELTS General Training', title: 'General Training Matching Headings', hub: '../pages/ielts-general.html', hubLabel: 'Back to IELTS General Hub' }
  };
  const requestedExam = new URLSearchParams(window.location.search).get('exam');
  const initialExam = Object.prototype.hasOwnProperty.call(EXAM_CONFIG, requestedExam) ? requestedExam : 'academic';
  const state = { datasets: {}, exam: initialExam, currentSetIndex: 0, selectedAnswers: {}, score: 0, totalAnswered: 0, answered: false, loaded: false, wrongAnswers: [] };
  const elements = {
    examCards: [...document.querySelectorAll('[data-exam]')], sessionTitle: document.querySelector('#matching-session-title'), examName: document.querySelector('#matching-exam-name'), setCounter: document.querySelector('#matching-question-counter'), scoreCount: document.querySelector('#matching-score-count'), progress: document.querySelector('#matching-progress'), progressBar: document.querySelector('#matching-progress-bar'), content: document.querySelector('#matching-question-area'), feedback: document.querySelector('#matching-feedback'), check: document.querySelector('#matching-check-answer'), next: document.querySelector('#matching-next-question'), restart: document.querySelector('#matching-restart'), hubLink: document.querySelector('#matching-hub-link'), hubLabel: document.querySelector('#matching-hub-label')
  };
  const createElement = (tag, className, text) => { const el = document.createElement(tag); if (className) el.className = className; if (text !== undefined) el.textContent = text; return el; };
  const currentSets = () => state.datasets[state.exam] || [];
  const currentSet = () => currentSets()[state.currentSetIndex];
  const headingText = (set, id) => { const heading = set.headings.find(item => item.id === id); return heading ? `${id}. ${heading.text}` : id; };
  const setProgress = value => { const safe = Math.max(0, Math.min(100, Math.round(value))); elements.progressBar.style.width = `${safe}%`; elements.progress.setAttribute('aria-valuenow', String(safe)); };
  const updateUrl = exam => { const url = new URL(window.location.href); url.searchParams.set('exam', exam); url.hash = 'practice-shell'; history.replaceState(null, '', `${url.pathname}${url.search}${url.hash}`); };
  const updateExamUi = () => { const config = EXAM_CONFIG[state.exam]; elements.examCards.forEach(card => { const active = card.dataset.exam === state.exam; card.classList.toggle('active', active); card.setAttribute('aria-pressed', String(active)); }); elements.sessionTitle.textContent = config.title; elements.examName.textContent = config.name; elements.hubLink.href = config.hub; elements.hubLabel.textContent = config.hubLabel; };

  const updateCheckState = () => { const set = currentSet(); elements.check.disabled = !set || set.paragraphs.some(paragraph => !state.selectedAnswers[paragraph.id]); };
  const createSelect = (set, paragraph) => {
    const select = createElement('select', 'matching-select'); select.dataset.paragraph = paragraph.id; select.setAttribute('aria-label', `Choose a heading for paragraph ${paragraph.id}`);
    const placeholder = createElement('option', null, 'Select a heading…'); placeholder.value = ''; select.append(placeholder);
    set.headings.forEach(heading => { const option = createElement('option', null, `${heading.id}. ${heading.text}`); option.value = heading.id; select.append(option); });
    select.addEventListener('change', () => { state.selectedAnswers[paragraph.id] = select.value; updateCheckState(); });
    return select;
  };
  const renderSet = () => {
    const sets = currentSets(); const set = currentSet(); if (!set) { showResults(); return; }
    state.selectedAnswers = {}; state.answered = false; updateExamUi();
    elements.setCounter.textContent = `Set ${state.currentSetIndex + 1} of ${sets.length}`; elements.scoreCount.textContent = `${state.score} / ${state.totalAnswered}`; setProgress((state.currentSetIndex / sets.length) * 100); elements.feedback.hidden = true; elements.feedback.replaceChildren(); elements.check.hidden = false; elements.check.disabled = true; elements.next.hidden = true; elements.next.textContent = 'Next Set →';
    const top = createElement('div', 'matching-content-top'); top.append(createElement('span', 'matching-difficulty', set.difficulty), createElement('span', 'matching-set-type', '4 paragraphs · 6 headings'));
    const title = createElement('div', 'matching-passage-title'); title.append(createElement('span', null, 'Mini reading passage'), createElement('h3', null, set.passageTitle), createElement('p', null, set.instructions));
    const bank = createElement('section', 'matching-heading-bank'); bank.setAttribute('aria-label', 'Available headings'); bank.append(createElement('h4', null, 'Heading Bank'));
    const headingList = createElement('ol'); set.headings.forEach(heading => { const item = createElement('li', 'matching-heading-item'); item.append(createElement('span', null, heading.id), createElement('p', null, heading.text)); headingList.append(item); }); bank.append(headingList);
    const passage = createElement('div', 'matching-passage-card');
    set.paragraphs.forEach(paragraph => { const row = createElement('section', 'matching-match-row'); row.dataset.paragraph = paragraph.id; const card = createElement('div', 'matching-paragraph-card'); card.append(createElement('span', 'matching-paragraph-label', `Paragraph ${paragraph.id}`), createElement('p', null, paragraph.text)); const control = createElement('div', 'matching-select-wrap'); control.append(createElement('label', null, `Heading for paragraph ${paragraph.id}`), createSelect(set, paragraph)); row.append(card, control); passage.append(row); });
    elements.content.replaceChildren(top, title, bank, passage);
  };
  const checkAnswers = () => {
    if (state.answered || elements.check.disabled) return; const set = currentSet(); let setScore = 0; state.answered = true;
    set.paragraphs.forEach(paragraph => { const selected = state.selectedAnswers[paragraph.id]; const correct = selected === paragraph.answer; if (correct) { state.score += 1; setScore += 1; } else { state.wrongAnswers.push({ setNumber: state.currentSetIndex + 1, paragraphLetter: paragraph.id, passageTitle: set.passageTitle, studentHeading: headingText(set, selected), correctHeading: headingText(set, paragraph.answer), explanation: paragraph.explanation }); } state.totalAnswered += 1; const row = elements.content.querySelector(`.matching-match-row[data-paragraph="${paragraph.id}"]`); row.classList.add(correct ? 'correct' : 'wrong'); const select = row.querySelector('.matching-select'); select.disabled = true; const explanation = createElement('div', 'matching-explanation'); explanation.append(createElement('strong', null, correct ? 'Correct match' : `Correct: ${headingText(set, paragraph.answer)}`), createElement('p', null, paragraph.explanation)); row.append(explanation); });
    elements.scoreCount.textContent = `${state.score} / ${state.totalAnswered}`; setProgress(((state.currentSetIndex + 1) / currentSets().length) * 100); elements.feedback.replaceChildren(createElement('strong', null, `${setScore} of ${set.paragraphs.length} matches correct.`), createElement('span', null, 'Review each explanation before continuing.')); elements.feedback.hidden = false; elements.check.hidden = true; elements.next.hidden = false; elements.next.textContent = state.currentSetIndex === currentSets().length - 1 ? 'View Results →' : 'Next Set →'; elements.next.focus();
  };
  const nextSet = () => { if (!state.answered) return; if (state.currentSetIndex >= currentSets().length - 1) { showResults(); return; } state.currentSetIndex += 1; renderSet(); elements.content.focus(); };
  const resultMessage = percentage => { if (percentage >= 90) return 'Excellent main-idea control. You are separating paragraph purpose from supporting detail very reliably.'; if (percentage >= 70) return 'Strong work. Review the headings that were too broad, too narrow, or attached to a detail.'; if (percentage >= 50) return 'A useful start. Summarize each paragraph in your own words before reading the headings.'; return 'Keep building the method. Focus on what the whole paragraph is doing, not one familiar phrase.'; };
  const makeStat = (label, value, className = '') => { const stat = createElement('div', `matching-review-stat ${className}`.trim()); stat.append(createElement('span', null, label), createElement('strong', null, value)); return stat; };
  const buildMistake = mistake => { const card = createElement('article', 'matching-mistake-card'); const meta = createElement('div', 'matching-mistake-meta'); meta.append(createElement('span', null, `Set ${mistake.setNumber} · Paragraph ${mistake.paragraphLetter}`), createElement('span', null, mistake.passageTitle)); const answers = createElement('div', 'matching-mistake-answers'); const student = createElement('div'); student.append(createElement('span', null, 'Your heading'), createElement('strong', 'wrong', mistake.studentHeading)); const correct = createElement('div'); correct.append(createElement('span', null, 'Correct heading'), createElement('strong', 'correct', mistake.correctHeading)); answers.append(student, correct); card.append(meta, answers, createElement('p', 'matching-mistake-explanation', mistake.explanation)); return card; };
  const buildReview = (total, percentage) => { const review = createElement('section', 'matching-review'); review.append(createElement('h4', null, 'Session Review')); const grid = createElement('div', 'matching-review-grid'); grid.append(makeStat('Total matches', String(total)), makeStat('Correct matches', String(state.score), 'correct'), makeStat('Wrong matches', String(state.wrongAnswers.length), 'wrong'), makeStat('Accuracy', `${percentage}%`, 'accuracy')); review.append(grid, createElement('p', 'matching-review-message', resultMessage(percentage))); if (state.wrongAnswers.length) { const list = createElement('div', 'matching-mistake-list'); list.append(createElement('h4', null, 'Review Mistakes')); state.wrongAnswers.forEach(mistake => list.append(buildMistake(mistake))); review.append(list); } return review; };
  const showResults = () => { const total = currentSets().reduce((sum, set) => sum + set.paragraphs.length, 0); const percentage = total ? Math.round((state.score / total) * 100) : 0; elements.setCounter.textContent = 'Session complete'; elements.scoreCount.textContent = `${state.score} / ${total}`; setProgress(100); const result = createElement('div', 'matching-result'); result.append(createElement('span', 'section-kicker', 'Heading sets complete'), createElement('strong', 'matching-result-score', `${state.score}/${total}`), createElement('h3', null, `${percentage}% correct`), createElement('small', null, 'Restart to practise the same mini passages again.')); elements.content.replaceChildren(result, buildReview(total, percentage)); elements.feedback.hidden = true; elements.feedback.replaceChildren(); elements.check.hidden = true; elements.next.hidden = true; elements.restart.focus(); };
  const resetSession = () => { state.currentSetIndex = 0; state.selectedAnswers = {}; state.score = 0; state.totalAnswered = 0; state.answered = false; state.wrongAnswers = []; renderSet(); };
  const showLoadingState = () => { elements.restart.disabled = true; elements.check.hidden = true; elements.next.hidden = true; elements.setCounter.textContent = 'Loading set…'; elements.scoreCount.textContent = '0 / 0'; setProgress(0); const loading = createElement('div', 'practice-loading'); loading.append(createElement('span', 'practice-loader'), createElement('p', null, `Loading ${EXAM_CONFIG[state.exam].name} heading sets…`)); elements.content.replaceChildren(loading); elements.feedback.hidden = true; };
  const showLoadingError = exam => { elements.restart.disabled = true; elements.sessionTitle.textContent = 'Practice data unavailable'; elements.examName.textContent = 'Please retry'; elements.setCounter.textContent = 'Not loaded'; elements.scoreCount.textContent = '0 / 0'; setProgress(0); const error = createElement('div', 'practice-load-error'); error.append(createElement('span', 'practice-load-error-icon', '!'), createElement('h3', null, 'We could not load the heading sets.'), createElement('p', null, `The ${EXAM_CONFIG[exam].name} dataset could not be loaded. Please run Exam Verse through a local web server and try again.`)); const retry = createElement('button', 'button button-secondary', 'Try Loading Again'); retry.type = 'button'; retry.addEventListener('click', () => loadExamDataset(exam)); error.append(retry); elements.content.replaceChildren(error); elements.feedback.hidden = true; elements.check.hidden = true; elements.next.hidden = true; };
  const loadJson = async path => { const response = await fetch(path, { cache: 'no-store' }); if (!response.ok) throw new Error(`Could not load ${path} (${response.status})`); const data = await response.json(); if (!Array.isArray(data) || !data.length) throw new Error(`No sets found in ${path}`); const active = data.filter(item => item.status === 'active'); if (!active.length) throw new Error(`No active sets in ${path}`); active.forEach(set => { if (!Array.isArray(set.headings) || !set.headings.length || !Array.isArray(set.paragraphs) || !set.paragraphs.length) throw new Error(`Invalid headings or paragraphs in ${set.id}`); const headingIds = set.headings.map(heading => heading.id); set.paragraphs.forEach(paragraph => { if (!headingIds.includes(paragraph.answer)) throw new Error(`Invalid answer '${paragraph.answer}' in ${set.id}, paragraph ${paragraph.id}`); }); }); return active; };
  async function loadExamDataset(exam) { if (!EXAM_CONFIG[exam]) return; if (state.datasets[exam]) { if (state.exam === exam) { state.loaded = true; elements.restart.disabled = false; resetSession(); } return; } if (state.exam === exam) { state.loaded = false; showLoadingState(); } try { const data = await loadJson(DATA_PATHS[exam]); state.datasets[exam] = data; if (state.exam !== exam) return; state.loaded = true; elements.restart.disabled = false; resetSession(); } catch (error) { console.error(`Exam Verse Matching Headings loading error for ${exam}:`, error); if (state.exam === exam) showLoadingError(exam); } }
  const switchExam = exam => { if (!EXAM_CONFIG[exam]) return; state.exam = exam; updateUrl(exam); updateExamUi(); if (state.datasets[exam]) { state.loaded = true; elements.restart.disabled = false; resetSession(); } else { loadExamDataset(exam); } };
  elements.examCards.forEach(card => card.addEventListener('click', () => { switchExam(card.dataset.exam); document.querySelector('#practice-shell').scrollIntoView({ behavior: 'smooth', block: 'start' }); })); elements.check.addEventListener('click', checkAnswers); elements.next.addEventListener('click', nextSet); elements.restart.addEventListener('click', resetSession); updateExamUi(); loadExamDataset(state.exam);
})();
